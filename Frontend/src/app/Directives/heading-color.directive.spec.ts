import { HeadingColorDirective } from './heading-color.directive';

describe('HeadingColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingColorDirective();
    expect(directive).toBeTruthy();
  });
});

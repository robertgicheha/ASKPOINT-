import { ShortQuestionPipe } from './short-question.pipe';

describe('ShortQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
